# Docker Environment Configuration for GoodDeeds
# Copy this file to .env.docker and update with your values before running docker-compose

# Database Configuration
DB_USER=gooddeeds_user
DB_PASSWORD=gooddeeds_password
DB_NAME=gooddeeds

# Database URL (automatically constructed in docker-compose)
DATABASE_URL=postgresql://gooddeeds_user:gooddeeds_password@postgres:5432/gooddeeds

# Next.js Environment
NODE_ENV=production
NEXTAUTH_URL=http://localhost:3000

# Security Keys (CHANGE THESE IN PRODUCTION!)
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production
JWT_SECRET=your-jwt-secret-change-in-production

# Optional: API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
